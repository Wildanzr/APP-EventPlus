import React from 'react'

import { Routes, Route, Navigate } from 'react-router-dom'

const AppRoutes = () => {
  return (
        <Routes>
            <Route path="/" />
        </Routes>
  )
}

export default AppRoutes
